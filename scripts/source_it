#!/bin/tcsh

# This script sets up a proper environment for simulations. Assumes a TCSH environment.

set noclobber
alias iverilog "iverilog -gstrict-ca-eval -Wall -g2005 -gio-range-error -gstrict-expr-width -Winfloop"
alias clean "rm *.out *.vcd *.log" 
