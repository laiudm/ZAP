# This script sets up a proper environment for simulations
set noclobber
alias iverilog "iverilog -I$ZAP_HOME/includes -gstrict-ca-eval -Wall -g2012 -gio-range-error -gstrict-expr-width" 
set prompt="ZAP_ENV :: %/ >"
